# --- Core web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# --- PDF text extraction (SRS 5.3: pdfplumber / PyPDF2) ---
pdfplumber==0.11.1
PyPDF2==3.0.1

# --- LLM providers (SRS 5.3: OpenAI GPT-4o/mini or Google Gemini) ---
openai==1.35.3
google-generativeai==0.7.1

# --- Agent orchestration (SRS 5.3: CrewAI or LangChain/LangGraph) ---
# Pick ONE of these once the agent implementation starts — both are listed
# here as options; uncomment whichever the team decides to use.
# crewai==0.35.8
# langchain==0.2.6
# langgraph==0.1.5

# --- HTTP client (for calling OpenAI/Gemini REST APIs directly if not
# using their SDKs above) ---
httpx==0.27.0

# --- Dev/testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
